除了系统属性外,可以自己定义Seter的属性!存在在Seter->data中

    $s = new \Seter\Seter();
    $s->mdtr = 'tem';
    print_r($s->data);

>像数组一样访问

$s = new \Seter\Seter();
$s['asdf'] = 123;
echo count($s);
var_dump($s->data);